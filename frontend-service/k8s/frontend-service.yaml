apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend-app # Ye deployment ke label se match hona chahiye
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer # Ye AWS se ek Public URL (DNS) nikaal kar dega